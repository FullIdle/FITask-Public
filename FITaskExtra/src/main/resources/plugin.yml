name: ${project.name}
main: ${project.group}
version: ${project.version}
api-version: 1.16
depend:
  - FITask
  - PlaceholderAPI
  - FICore
authors: [GSQ_FI]
commands:
  fitaskextra:
    description: main cmd